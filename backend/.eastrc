const dotenv = require('dotenv');
const path = require('path');

dotenv.config();

module.exports = {
  dir: path.join(__dirname, 'migrations'),
  template: path.join(__dirname, 'migrations', '.template.js'),
}
